const logger = require('../../../loggerConfig/logger')

class ElasticSearchService {
    static async bulkInsert({index, vedioContentList}) {
        const bulkPayload = [];
        
    }
}
  
module.exports = ElasticSearchService